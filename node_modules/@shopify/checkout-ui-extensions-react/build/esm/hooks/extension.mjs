import { useApi } from './api.mjs';

/**
 * Returns the metadata about the extension.
 */
function useExtension() {
  return useApi().extension;
}

/**
 * Returns the metadata about the extension.
 */
function useExtensionData() {
  return useExtension();
}

export { useExtension, useExtensionData };
