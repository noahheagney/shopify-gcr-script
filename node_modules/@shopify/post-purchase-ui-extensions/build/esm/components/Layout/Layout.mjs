import { createRemoteComponent } from '@remote-ui/core';

/**
 * Layout is used to create macro layouts that responds to different media sizes.
 */
const Layout = createRemoteComponent('Layout');

export { Layout };
