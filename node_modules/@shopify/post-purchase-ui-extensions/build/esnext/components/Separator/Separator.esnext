import { createRemoteComponent } from '@remote-ui/core';

/**
 * Separators create visual separation between sections of an interface.
 */
const Separator = createRemoteComponent('Separator');

export { Separator };
